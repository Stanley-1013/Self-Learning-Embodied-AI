# Round-3 Fresh-Eyes Audit: Ch09-14

**Date**: 2026-04-20
**Scope**: Fresh-eyes pass on Ch09-Ch14; verify Round-2 fixes and catch remaining blind spots.
**Error categories**: concept direction, math/physics, fact, algo hallucination, reasoning gap, terminology conflation.

Severity legend: CRITICAL (physics/math wrong that will propagate to code), HIGH (inconsistency or imprecise claim that could mislead), MEDIUM (minor imprecision worth fixing), LOW (nit).

---

## Ch09 — 動力學建模

### Round-2 fix verification

- **LMI triangle inequality** (line 471): now reads `I_jj + I_kk ≥ I_ii 對所有 {i,j,k} 的排列都成立`, plus pseudo-inertia equivalence note and tr(I) > λ_max weakening caveat. Traversaro, Prete & Nori 2016 + Wensing, Kim & Slotine 2017 attribution present. **Pass**.
- **Barbalat's Lemma + uniform continuity** (line 507): reads `s ∈ L^∞ ∩ L^2；再由 Barbalat's Lemma（V̇ 均勻連續）推得 s → 0`. V̇ uniform continuity is the correct prerequisite (bounded s, bounded ṡ ⇒ V̇ = -s^T K s uniformly continuous). **Pass**.
- **Frame-convention note** (line 117): covers $v_i$ in frame $i$, $R_i$ maps $i-1 \to i$, $r_i$ in frame $i$, gravity via $\ddot p_0 = -g$. **Pass** modulo one nit (see LOW finding below).

### Beyond round-2

- **CMM ordering convention** (line 233): explicitly states Pinocchio uses `[l; k]` (linear on top), Orin & Goswami 2008 uses `[k; l]`. Matches Pinocchio `Force` type `[linear; angular]` memory layout. **Pass**.

### Remaining findings

1. **LOW — frame-convention nit** (line 117 + line 120):
   - Note says: `$\hat{z} = [0,0,1]^T$ 代表 joint $i$ 自身 frame 的旋轉軸`.
   - Formula: $\omega_i = R_i^T (\omega_{i-1} + \dot{q}_i \hat{z})$.
   - For the sum `ω_{i-1} + \dot q_i \hat z` to be coordinate-consistent, `\hat z` must be expressed in frame $i-1$ (joint $i$'s axis resolved in frame $i-1$ = $[0,0,1]^T$ under standard DH because joint $i$'s axis aligns with $z_{i-1}$). Calling it "joint $i$ 自身 frame 的旋轉軸" slightly contradicts the "變數 $v_i$ 表達在 frame $i$" convention stated two lines earlier.
   - Suggested fix: `$\hat{z} = [0,0,1]^T$ 代表 joint $i$ 的旋轉軸（在 frame $i-1$ 中的 DH 表達）`.
   - Severity LOW — experienced readers will resolve from context, but the inconsistency is detectable on close reading.

2. **LOW — Stribeck + $\hat{\pi} \to \pi$** (outside round-2 scope): Slotine-Li Lyapunov block (line 507) ends with `但不保證 $\hat{\pi} \to \pi$`. The PE note follows. Clean.

---

## Ch10 — 基礎路徑規劃

### Round-2 fix verification

- **Chebyshev/Octile selection rule** (line 326): `斜向代價 = 1 → Chebyshev；斜向代價 = √2（Nav2 預設）→ Octile`. **Pass**.
- **Nav2 InflationLayer three-tier mapping** (line 942): `254 (LETHAL at cell) / 253 (INSCRIBED_INFLATED_OBSTACLE at r_inscribed) / 252 · exp(…) (inflation ring)`. `(INSCRIBED_INFLATED_OBSTACLE - 1) = 252` — correct. **Pass**.

### Beyond round-2

- **Dubins 6-word set** (line 169): `{LSL, RSR, LSR, RSL, LRL, RLR}`, CSC (first 4) vs CCC (last 2). **Pass**.
- **Reeds-Shepp 46 vs 48 wording** (line 171): `原 paper（Reeds & Shepp 1990）列 48 candidate words，其中 46 種 distinct pattern（學界兩種引法都常見）` — accurate. Practitioners often say "48 words with 46 distinct after eliminating duplicates". **Pass**.

### Remaining findings

Zero new findings.

---

## Ch11 — 軌跡最佳化

### Round-2 fix verification

- **Single "深入：TrajOpt 三框架對比" block** — grep confirms only one `深入：TrajOpt 三框架對比` summary (line 91). Line 669 is a different TrajOpt details block (`TrajOpt → 閉環控制橋接`). **Pass** — duplicate successfully removed.
- **iLQR backward-pass simplified forms** (lines 588-594): provides both the expanded Riccati form and the simplified `V_x = Q_x + K^T Q_u`, `V_{xx} = Q_{xx} + K^T Q_{ux}` after substituting $k = -Q_{uu}^{-1} Q_u$, $K = -Q_{uu}^{-1} Q_{ux}$. Includes the caveat about sign consistency. **Pass**.

### Beyond round-2

- **Min-snap QP matrix factorial** (line 253): `Q_{ij} = (i!/((i-4)!)) · (j!/((j-4)!)) · T^{i+j-7} / (i+j-7)` for `i, j ≥ 4`. Derivation: $p^{(4)}(t) = \sum_{k\geq 4} c_k \frac{k!}{(k-4)!} t^{k-4}$, so $\int_0^T (p^{(4)})^2 dt = \sum_{i,j \geq 4} c_i c_j \frac{i!}{(i-4)!} \frac{j!}{(j-4)!} \int_0^T t^{i+j-8} dt$, and $\int_0^T t^{i+j-8} dt = T^{i+j-7}/(i+j-7)$. **Pass** — factorial and exponent correct.
- **Differential Flatness $\psi$ ambiguity** (line 227): explicitly clarifies `這裡的 $\psi$ 是歐拉 RPY 的 yaw` and notes roll/pitch are algebraically recovered (not independent flat outputs). **Pass**.

### Remaining findings

Zero new findings.

---

## Ch12 — 動態避障

### Round-2 fix verification

- **Torque null-space projector consistency** — **FAIL: inconsistency remains**.
  - Line 113 (core-concept formula block):
    ```
    τ_null = (I - J^+ J) · τ_avoid
    ```
    This is the VELOCITY-space null-space projector `(I - J^+ J)` applied to a torque quantity.
  - Lines 682 and 707 (deeper details + C++ code):
    ```
    τ_null = (I - J^T · J^{+T}) · τ_avoid
    ```
    Correct torque-space projector.
  - Round-2 fix updated the deep block and the code, but **the top-level formula at line 113 was not updated**. The two projectors are transposes, not equal — applying velocity-form projector to torque generally does NOT zero end-effector wrench.
  - The prose note at line 682 explicitly calls out the distinction: `速度零空間投影 $(I - J^+ J)$ 用於 $\dot q$、扭矩零空間投影 $(I - J^\top J^{+\top})$ 才是 $\tau$ 用的，兩者互為轉置`. This makes the line-113 formula strictly wrong (not just a convention choice).
  - Severity **HIGH**. Suggested fix: replace line 113 formula with `τ_null = (I - J^T J^{+T}) · τ_avoid`.

### Beyond round-2

1. **ORCA half-plane normal direction** (line 224): `$\mathbf{n}$ 取作 $\mathbf{u}$ 的單位向量（指向 VO 外側）` with a counter-example note. Accurate enough for the common case (v_A − v_B inside VO so u ≠ 0); the VO-outside case u = 0 is glossed but not incorrect. **Pass**.
2. **CBF direction** (lines 88, 96, 410): `ḣ + α(h) ≥ 0` and the QP form `L_f h + L_g h · u + α h ≥ 0`. Standard Ames convention, matches "safety set forward invariant". **Pass**.

### Remaining findings

1. **HIGH — velocity-form null-space projector applied to torque at line 113**. See above. Fix required.

---

## Ch13 — PID 控制調諧

### Round-2 fix verification

- **Gain Scheduling formula** (line 491): `K_p^{(i)}(q) = K_{p_0}^{(i)} · M_{ii}(q) / M_{ii,0}`. The earlier `det(M)` normalization (mathematically dubious for non-square or poorly-conditioned joints) has been replaced with per-axis diagonal normalization. **Pass**.
- **Tustin frequency-warping note** (line 433): `虛軸 $j\omega$ 會發生 frequency warping，關鍵頻點實作時常用 pre-warping 校正`. **Pass**.

### Beyond round-2

- **Anti-windup three strategies internal consistency** (lines 65-122):
  - Strategy 1 Conditional Integration: freeze integral on saturation ✓
  - Strategy 2 Back-Calculation: $\dot{I} = K_i e + (1/T_t)(u_{sat} - u_{raw})$ with $T_t = \sqrt{T_i T_d}$ ✓
  - Strategy 3 Clamping: hard clamp integral value ✓
  - Cascade Tracking Anti-Windup: inner-loop saturation propagates to outer loop ✓
  - Selection table (lines 112-118) matches prose. **Pass**.

### Remaining findings

Zero new findings.

---

## Ch14 — 力/阻抗控制

### Round-2 fix verification

- **Adjoint direction in formula block** (line 275): `$W_B = \mathrm{Ad}_{T_{AB}}^T \cdot W_A$`. Correct direction (opposite to twist `V_A = Ad V_B`). **Pass**.
- **Pure-rotation sanity check** (line 278): `$\mathrm{Ad}^T = \mathrm{diag}(R^T, R^T)$，所以 $W_B = R^T \cdot W_A$`, with explicit counter-example of reversed direction yielding `W_A = R^T W_B` (wrong). **Pass**.
- **Adjoint direction in prose** (line 272): `wrench 以 $\mathrm{Ad}^T$ 向『相反方向』變換` — matches formula. **Pass**.
- **Stiffness congruence $K_{tcp}$** (lines 303, 358-359): `K_tcp = Ad_T^T · K_flange · Ad_T` sandwich form. Line 278 prose explains: `剛度共軛 K_tcp = Ad_T^T K_flange Ad_T 的 sandwich 形式對此方向慣例是穩健的` — the congruence structure maps stiffness consistently under either direction choice for twist/wrench because stiffness is bilinear. **Pass**.

### Beyond round-2

- **Raibert Selection Matrix S + (I-S) orthogonality** (line 62): `S 是 6×6 對角矩陣，每個對角元素 0 或 1 … S 與 (I - S) 在數學上絕對正交`. For diagonal 0/1 S: $S^2 = S$ (idempotent projector), $S(I-S) = S - S^2 = 0$ ⇒ product is zero matrix. Claim is mathematically correct. The term "正交" here means the two projectors have zero product (sometimes called "orthogonal complementary projectors"), not that $S$ is an orthogonal matrix in the $O(n)$ sense. Given the immediately-following explanation `完美切分 6D 任務空間，避免『同一維度又要位置 = 10 又要力 = 5N』的超定問題`, the intended meaning is clear. **Pass**.

### Remaining findings

Zero new findings.

---

## Summary

| Chapter | CRITICAL | HIGH | MEDIUM | LOW | Round-2 fixes verified |
|---------|:---:|:---:|:---:|:---:|:---:|
| Ch09    | 0 | 0 | 0 | 1 | 3/3 pass |
| Ch10    | 0 | 0 | 0 | 0 | 2/2 pass |
| Ch11    | 0 | 0 | 0 | 0 | 2/2 pass |
| Ch12    | 0 | 1 | 0 | 0 | 0/1 pass (null-space fix incomplete) |
| Ch13    | 0 | 0 | 0 | 0 | 2/2 pass |
| Ch14    | 0 | 0 | 0 | 0 | 3/3 pass |

**Total new findings**: 1 HIGH + 1 LOW = 2.

### Action items

1. **Ch12 / HIGH / line 113**: replace velocity-form null-space projector with torque-form to match round-2 fix at lines 682/707 and the accompanying prose. Suggested fix:
   ```
   τ_null = (I − J^T J^{+T}) · τ_avoid
   ```
   (or equivalently add a parenthetical cross-reference pointing readers to the deep block, but the inconsistency is the type of error a careful reader will catch immediately).

2. **Ch09 / LOW / line 117**: clarify that `$\hat{z} = [0,0,1]^T$` is the joint-$i$ axis expressed *in frame $i-1$'s coordinates* (standard DH), not in frame $i$'s coordinates — otherwise the vector sum `ω_{i-1} + \dot q_i \hat z` is coordinate-inconsistent with the "variables subscripted $i$ live in frame $i$" rule stated earlier in the same note.
