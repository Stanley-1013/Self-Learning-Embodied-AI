# Round-3 Fresh-Eyes Audit: Ch15-22

**Scope**: Verify round-2 fixes + catch remaining blind spots across 6 error categories (concept direction, math/physics, fact, algo hallucination, reasoning gap, terminology conflation).

**Methodology**: Re-read each chapter end-to-end. Cross-check formulas against canonical references. Flag only substantive issues.

---

## Ch15 — Model Predictive Control

### Round-2 fixes — verified

- **NMPC SQP O(n^3)** — Line 86 correctly states "KKT 因式分解（$O(n^3)$）+ Hessian/Jacobian 裝配（由 RNEA/CRBA...$O(n^2) \sim O(n^3)$）". No residual O(n^4) claim. PASS.
- **Offset-free canonical state-disturbance form** — Line 500-504 gives the canonical augmented-state form and explicitly warns: "等價的輸出擾動模型是 $x_{k+1} = Ax_k + Bu_k, \; y_k = Cx_k + d_k$ — 兩者擇一即可，**不要同時放 $B_d d_k$ 與 $y = Cx + d$**（會過參數化、通常不可觀測）". PASS.
- **HOCBF relative-degree caveat** — Line 247 "上式僅在 $h$ 相對階 = 1 時有效…若 $h$ 相對階 > 1…需用 **HOCBF（Xiao & Belta 2019）**". PASS.

### Beyond round-2

- **Tube MPC constraint tightening direction** — Line 331 "把原硬約束向內收縮一個 tube 半徑", formula $\bar{x} \in X \ominus Z$ at line 335. Minkowski difference = shrinking. CORRECT.
- **CBF `ḣ + α·h ≥ 0`** — Both forms shown (line 238 `\dot{h} \ge -\alpha h`, line 245 `\dot{h} + \alpha h \ge 0`). Mathematically equivalent and canonical. CORRECT.
- **MPPI weight formula** — Line 968 `w_i = \exp(-S(\tau_i)/\lambda) / \sum_j \exp(-S(\tau_j)/\lambda)`. Williams 2017 canonical form. CORRECT.
- **Lie group log map** — Line 480 `e_R = \log(R_{\text{target}}^T \cdot R_{\text{current}})^\vee`. Convention is defensible (error in target's body frame); works as long as feedback sign is consistent. Line 487-489 manif C++ snippet uses `X_target.inverse() * X_current`, matching the math. No defect.

### Remaining findings — Ch15

**None substantive.** One minor cosmetic note: the Lie-group error convention (target-frame vs current-frame log-map) is not universal — some literature uses `log(R_current^T R_target)` (opposite sign). The chapter is internally consistent and the prose explanation ("要繞哪個軸轉多少角度對齊") works with either interpretation as long as the feedback gain sign is flipped accordingly. This is not an error, just a convention choice.

---

## Ch16 — Visual Servoing

### Round-2 fixes — verified

- **PBVS formula with R^T** — Line 80:
  ```
  v_c = -λ [R^T · ^{c*}t_c ; θu]
  ```
  `R^T` is explicitly in the formula block. PASS.
- **Prose explanation** — Line 83: "**`R^T` 至關重要** — 它把平移向量從目標座標系 $c^*$ 轉回相機自身座標系 $c$…(Chaumette & Hutchinson 2006, Eq. 23-27 的第二種選擇)". Citation present, coordinate-frame reasoning correct. PASS.
- **Coplanar-not-collinear fix** — Line 165 "4 點只需共面不共線即可（Chaumette 經典結果），不要求非共面". PASS.

### Beyond round-2

- **Image Jacobian sign conventions** — Line 58-62:
  ```
  L = [ -1/Z   0    x/Z   xy        -(1+x²)   y ;
         0    -1/Z  y/Z   1+y²       -xy     -x ]
  ```
  Matches Chaumette & Hutchinson 2006 Eq. 12 exactly (with convention `ṗ = -v - ω×P`, which yields the leading `-1/Z` signs). CORRECT.
- Physical-meaning annotations at lines 65-67 are accurate: translation columns scale with 1/Z; rotation-column coefficients don't display Z.

### Remaining findings — Ch16

**None substantive.**

---

## Ch17 — SLAM and Mapping

### Round-2 fixes — verified

- **ICP point-to-plane as production LiDAR variant** — Line 72: "上式是 **point-to-point ICP**；實務 LiDAR SLAM（LOAM / LIO-SAM / FAST-LIO2）多用 **point-to-plane** 變體(將殘差投影到局部平面法向)…". PASS.
- **IMU corridor limitation clarified** — Line 362-363: "IMU 預積分在**短時段內**提供軸向位移先驗 + 陀螺儀防偏航漂移；但 IMU 本身(加速度計二次積分有 bias 漂移)**不能獨立解除長期軸向退化**，仍需 loop closure / UWB / 人工路標做絕對約束". PASS.

### Beyond round-2

- **Bundle Adjustment / Schur complement** — Chapter does not specifically cover BA; Schur complement not discussed. Given the chapter's scope (SLAM survey, not Visual SLAM deep-dive), this omission is acceptable. g2o / GTSAM / Ceres are mentioned as the backend optimizers — BA details can live in a dedicated VSLAM chapter if ever added.
- **Graph-SLAM Hessian sparsity / sparse Cholesky** — Lines 90-131. Claim "O(n) or O(n^1.5)" on line 125 and "接近 O(n)" on line 424. For 2D planar SLAM with limited loops this is accurate; for 3D SLAM with many loop closures the cost can be higher. Not strictly wrong, mildly optimistic phrasing. LOW severity.

### Remaining findings — Ch17

- **LOW**: Sparse-Cholesky complexity phrasing (line 125, 424) is slightly optimistic for general 3D SLAM with dense loop-closure graphs. Not a blocker for the chapter's scope.

---

## Ch18 — RL MDP Basics

### Round-2 fixes — verified

- **DQN terminated mask (not done)** — Lines 400-416 show the corrected flow:
  - Line 404: `buffer.push(state, action, reward, next_state, float(terminated))` — stores terminated, not done. PASS.
  - Line 410: `s, a, r, ns, t = buffer.sample(batch_size)   # t = terminated mask, NOT done` — comment explicit. PASS.
  - Line 416: `target_q = r + gamma * max_next_q * (1 - t)   # 只遮 terminated，不遮 truncated` — bootstrap mask is terminated. PASS.
  - Lines 400-403 + 222-223 + 266 have repeated comments explaining the terminated-vs-truncated distinction. PASS.
- Tabular Q-Learning and SARSA code also corrected (lines 224, 267, 310).

### Beyond round-2

- **Bellman equation consistency** — V form (line 62), Q form (line 70), and V↔Q link (line 28: `V(s) = max_a Q(s,a)` under optimal policy; line 105 under stochastic policy: `V^π(s) = Σ_a π(a|s) Q^π(s,a)`). All consistent.
- **MDP 5-tuple** — Line 22: `(S, A, P, R, γ)`. Standard. CORRECT.
- **TD error** — Line 78 Q-Learning, line 87 SARSA. Both correct.
- **γ effective horizon** — Line 452 addresses the `1/(1-γ) = 100` vs `0.99^100 ≈ 0.37` coexistence and notes both are defensible heuristics. Good hygiene.

### Remaining findings — Ch18

**None substantive.**

---

## Ch19 — DRL (PPO, SAC, DDPG)

### Round-2 fixes — verified

- **PPO clip intuition (ratio, not action deviation)** — Line 185: "安全繩把新舊策略的**機率比** $r_t = \pi_\theta / \pi_{\text{old}}$ 限制在 $[1-\varepsilon, 1+\varepsilon]$…**注意 clip 的是機率比，不是動作偏差或 KL 散度**". PASS.
- **TD3 target smoothing as critic regularizer** — Line 50 table row: "Gaussian 行為噪聲（target smoothing 屬 critic 正則化）". PASS.
- **DDPG citation** — Line 115: "DDPG (Lillicrap et al., arXiv 2015 / ICLR 2016, DeepMind)". PASS.

### Beyond round-2

- **PPO clip formula** — Line 69: `min(r_t · Â_t, clip(r_t, 1-ε, 1+ε) · Â_t)` with `r_t = π_θ / π_old`. Matches Schulman 2017 Eq. 7 exactly. CORRECT.
- **SAC max-entropy objective** — Line 79: `J(π) = E_π [Σ γ^t (r_t + α H(π(·|s_t)))]`. Sign on entropy is **positive** (bonus, not penalty). Matches Haarnoja 2018 canonical form. CORRECT.
- **GAE formula** — Line 95: `Â_t = Σ (γλ)^l δ_{t+l}`. Schulman 2016 canonical. CORRECT.
- **Policy gradient** — Line 61: `∇J = E[∇log π · Q]`. CORRECT.
- **TD error** — Line 87. CORRECT.

### Remaining findings — Ch19

**None substantive.**

---

## Ch20 — Imitation Learning & DAgger

### Round-2 fixes — verified

- **Ross & Bagnell AISTATS 2010 for BC T² bound** — Line 52-58 (formula) + Line 101 ("Ross & Bagnell (AISTATS 2010, 'Efficient Reductions for Imitation Learning')") + Line 461 (延伸閱讀 citation). PASS.
- **DAgger β-mixing schedule note** — Lines 68-69: "原論文的 $\beta$-mixing schedule…Ross et al. 2011 定義的 rollout 策略其實是 $\pi_i = \beta_i \pi_E + (1 - \beta_i)\hat{\pi}_i$…實務上大多數實作…直接取 $\beta = 0$ 純用學生動作，稱為 'pure DAgger'". PASS.

### Beyond round-2

- **ACT action chunking + temporal ensemble** — Line 183 correctly disambiguates: "Temporal ensemble（ACT 的推理時 trick，非 chunking 本身的性質）…單純 action chunking 不需要 temporal ensemble 也能用". Good separation of concerns.
- **Diffusion Policy** — Forward process (line 152), reverse process (line 160), training loss (line 168). All match DDPM canonical form (Ho et al. 2020) and Diffusion Policy paper (Chi et al. 2023). CORRECT.
- **GAIL min-max** — Line 81. Matches Ho & Ermon 2016. CORRECT.
- **BC T² bound** — Line 55 `J(π_E) - J(π_θ) ≤ T²·ε + O(T)`. Matches Ross-Bagnell AISTATS 2010. CORRECT.

### Remaining findings — Ch20

**None substantive.**

---

## Ch21 — Sim-to-Real Transfer

### Round-2 fixes — verified

- **Rudin 2021 labeled as parallel-RL/DR (not Teacher-Student)** — Line 515: "Rudin et al. (ETH Zurich),《Learning to Walk in Minutes Using Massively Parallel Deep RL》(CoRL 2021)…**注意：這是並行 RL / DR 路線，不是 Teacher-Student**". PASS.
- **Lee 2020 + Miki 2022 as T-S canonical** — Line 516: "Lee et al., Science Robotics 2020 / Miki et al., Science Robotics 2022 — Teacher-Student + LSTM 隱式物理推斷的正宗範例". PASS.
- **UR5 latency (8ms cycle + 20-40ms e2e)** — Line 429: "UR5 的 RTDE servoJ 週期就是 ~8ms（125 Hz），但加上 ROS/網路/控制器管線後端到端常是 20–40ms". PASS.
- **Teacher-Student definition improved** — Line 34 now cites "Chen et al. 2020 'Learning by Cheating'" for privileged-learning framework and adds caveat "但這是設計選擇而非定義 — 某些任務的 Student 可以是單步前饋網路". PASS.

### Beyond round-2

- **DR expectation objective** — Line 57: `π* = argmax_π E_{ξ~p(ξ)}[Σ γ^t r(s_t, a_t; ξ)]`. CORRECT.
- **SysID least-squares** — Line 65. CORRECT; persistent-excitation caveat mentioned line 68, 404.
- **DR parameter scope** — Table at line 117-127 gives reasonable ranges (mass ±30%, friction ±50%, delay 0-80ms). Aligned with OpenAI Dactyl + Rudin 2021 practice. REASONABLE.

### Remaining findings — Ch21

**None substantive.**

---

## Ch22 — Multimodal LLM Robotics (VLA)

### Round-2 fixes — verified

- **Fabricated scaling law REMOVED** — Lines 96-103 replaced with "VLA Scaling 的經驗觀察（非正式法則）" and explicit statement "**但目前沒有已驗證的閉式 scaling law**（不像 LLM 有 Kaplan/Chinchilla 那種曲線擬合）". PASS.
- **"21 institutions across 22 robot embodiments"** — Three occurrences verified:
  - Line 115 (table): "~1M episodes | 22 個機器人、多機構聯合" — lists 22 robots (matches embodiment count), flags multi-institution. Acceptable, though less explicit than "21 機構" elsewhere.
  - Line 123: "21 個機構、涵蓋 22 種 robot embodiments" — EXACT PHRASING MATCH.
  - Line 436: "21 機構、22 種 robot embodiments" — EXACT PHRASING MATCH.
  PASS (2/3 explicit matches; table row uses shorter phrasing but is not inconsistent).
- **RT-2 unseen failure rate ~38%** — Line 339: "成功率約 62%，即仍有 ~38% 失敗率". PASS.
- **OpenVLA multi-institution attribution** — Line 433: "OpenVLA (Stanford + UC Berkeley + TRI + MIT + Google DeepMind, 2024) — Kim et al. 領銜的多機構協作". PASS.

### Beyond round-2

- **RT-2 256-bin vocabulary token reuse** — Line 32: "RT-2 每維用 256 bins 離散化，並**重用 VLM vocabulary 中使用頻率最低的 256 個 token 作為 action token**（不新增 token、直接復用 Transformer head）". Matches RT-2 paper Section 3.2 (token reuse strategy). CORRECT.
- **World model loss** — Line 83 gives reconstruction + KL term. Matches DreamerV3 / RSSM canonical form. CORRECT.
- **SayCan affordance** — Line 75: `π(a|l,s) = P_LLM(a|l) · P_affordance(a|s)`. Matches Ahn et al. 2022. CORRECT.
- **Action tokenization formula** — Line 91: uniform binning with clipping. Matches RT-2 appendix. CORRECT.

### Remaining findings — Ch22

- **LOW**: Line 115 table row says "22 個機器人、多機構聯合" while prose elsewhere says "21 機構、22 embodiments". Not contradictory (22 embodiments ≈ 22 機器人 if counting distinct robot types), but slightly less precise than the explicit "21 institutions" phrasing in lines 123 and 436. A future polish could normalize this.

---

## Summary

| Chapter | Round-2 fixes verified | New findings | Overall |
|---------|------------------------|--------------|---------|
| Ch15 MPC | 3/3 | 0 | CLEAN |
| Ch16 Visual Servoing | 3/3 | 0 | CLEAN |
| Ch17 SLAM | 2/2 | 1 LOW (sparse Cholesky phrasing) | CLEAN |
| Ch18 RL basics | DQN terminated mask verified | 0 | CLEAN |
| Ch19 DRL | 3/3 | 0 | CLEAN |
| Ch20 IL & DAgger | 2/2 | 0 | CLEAN |
| Ch21 Sim2Real | 4/4 | 0 | CLEAN |
| Ch22 VLA | 4/4 | 1 LOW (table-row phrasing consistency) | CLEAN |

**All HIGH and MEDIUM round-2 fixes are visibly in place.** Only two LOW-severity polish opportunities flagged, neither a correctness issue:

1. Ch17 line 125/424: sparse-Cholesky cost phrasing is mildly optimistic for general 3D SLAM.
2. Ch22 line 115 table: "22 個機器人、多機構聯合" is less precise than the "21 機構、22 embodiments" phrasing used elsewhere in the chapter; no factual contradiction.

**No new CRITICAL or HIGH findings across Ch15-22.** The round-2 remediation work is successfully landed.

### Spot-check notes on formulas / facts (Ch15-22)

- **Ch15**: Lie-group error log-map direction is a convention; chapter is internally consistent.
- **Ch16**: Image Jacobian sign matches Chaumette & Hutchinson 2006 Eq. 12 convention.
- **Ch18**: V↔Q relationships correct under both optimal (max) and stochastic (expectation) formulations.
- **Ch19**: PPO clip formula, SAC max-ent objective, GAE formula all match canonical sources.
- **Ch20**: Diffusion Policy forward/reverse/loss match Ho 2020 + Chi 2023.
- **Ch21**: T-S definition now correctly framed as a privileged-learning design pattern, with LSTM-from-history explicitly marked as a common-but-not-universal design choice.
- **Ch22**: Scaling-law fabrication fully purged; no closed-form claim remains. Token-reuse trick (RT-2 256 lowest-freq tokens) correctly attributed.
