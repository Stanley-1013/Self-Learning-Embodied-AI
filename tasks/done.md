# 已完成任務

<!-- 每次完成 current.md 中的任務後，把任務摘要（含完成日期、產出檔案）移到這裡 -->

---

## TASK-20260418-01: 初始化 Docusaurus 專案
- **完成日期**: 2026-04-18
- **產出**:
  - `docusaurus.config.ts` — 中文 zh-Hant、`content/` 當 docs、`routeBasePath: content`、KaTeX (`remark-math` + `rehype-katex`)、CDN KaTeX CSS、關閉 blog、`markdown.format: 'detect'`
  - `sidebars.ts` — autogenerated sidebar 掃 `content/`
  - `package.json` — 改 name 為 `robotic-learning`；補 `dev` script alias
  - `src/pages/index.tsx` — 中文首頁、CTA 連到 `/content/example/intro` 與 `/progress`
  - `src/pages/progress.tsx` — 進度總覽占位頁（等 TASK-02 掛 Dashboard）
  - `src/components/README.md` — 列出後續任務要生的元件
  - `content/index.md` — 內容索引
  - `content/example/01-intro.md` — KaTeX 渲染驗證範例章節（通過 validate.sh）
- **驗證**:
  - `npm run build` 綠（無 broken links）
  - `npm start --port 3333`：landing/chapter/progress 三個路由 HTTP 200，KaTeX CSS 已從 CDN 載入
  - `bash quality/validate.sh` 全過（0 錯誤 0 警告）
- **備註**:
  - `create-docusaurus` 不接受非空目錄，改用 tmp dir scaffold 後 `cp -r` 回來
  - Docusaurus 預設把 `.md` 當 MDX，HTML 註解 `<!-- -->` 會炸 → 在 config 設 `markdown.format: 'detect'` 才讓 `.md` 走 CommonMark
  - 路由會自動剝 `01-` 前綴（`01-intro.md` → `/content/example/intro`），React 內部連結要用剝過前綴的路徑；Markdown 內部用 `.md` 副檔名讓 Docusaurus 自行解析
  - `content/intro.md` 不是章節，用 `content/index.md` 命名才會被 `validate.sh` 跳過

---

## TASK-20260418-02: 建立進度追蹤系統
- **完成日期**: 2026-04-18
- **產出**:
  - `src/lib/progress.ts` — localStorage store（`robotic-learning.progress.v1`）+ `useProgressEntry` / `useProgressMap` hook + 自訂事件 `robotic-learning:progress-updated` 同分頁同步
  - `src/components/ProgressToggle.tsx` + `.module.css` — 每章底部完成按鈕，BrowserOnly 包裝
  - `src/components/ProgressDashboard.tsx` + `.module.css` — 用 `useAllDocsData` 枚舉章節、依路徑第一段分群為書，每書顯示百分比 / segmented tick bar / 最後完成時間
  - `src/theme/DocItem/Footer/index.tsx` — swizzle wrap `@theme/DocItem/Footer`，每頁自動掛 ProgressToggle
  - `src/pages/progress.tsx` — 正式掛載 Dashboard
  - **設計 pass（editorial reference）**：`src/pages/index.tsx`（asymmetric masthead + numbered manifesto + 目次索引 + pull quote）、`src/css/custom.css`（暖紙/朱砂 palette、Fraunces + Inter Tight + JetBrains Mono、hairline rules、drop cap、章節 section counter）、`ProgressDashboard` 10rem 型態式百分比 + 目錄式 book cards
- **驗證**:
  - `npm run build` 綠、無 broken links
  - KaTeX 類別在 chapter SSR 仍存在（10 matches）
  - `bash quality/validate.sh` 全過
  - localStorage 切換/重整的瀏覽器級 E2E 延到 Playwright 引入後再補
- **備註**:
  - `ProgressToggle` 和 `ProgressDashboard` 都要包 `BrowserOnly`（`fallback={null}` / `fallback=<p>讀取進度中…</p>`），否則 SSR 階段 `window`/`localStorage` 會炸
  - swizzle wrap 路徑為 `src/theme/DocItem/Footer/index.tsx`，用 `@theme-original/DocItem/Footer` 重用原功能
  - Dashboard 依 `plugin.path`（routeBasePath）切出 book slug；若未來路徑結構變動（例如中間插入 version），`groupDocsByBook` 要跟著改
  - 自訂 CSS variable（`--ink`、`--paper`、`--accent`…）定義在 `src/css/custom.css`；編輯元件樣式時優先重用這些 token 以保持風格一致

---

## TASK-20260418-03: 建立內容管線模板
- **完成日期**: 2026-04-18
- **產出**:
  - `pipeline/templates/chapter.md.tmpl` — 純 Markdown scaffold（frontmatter + 8 必要段落標題 + `{{slot}}` 占位），14 個對稱 slot
  - `pipeline/templates/quiz.md.tmpl` — `<details>` 單題模板 + 使用註解（可重複展開成多題）
  - `pipeline/scripts/validate_chapter.py` — Python 版驗證，等價於 `quality/validate.sh`；exports `validate_file` / `validate_many` / `ValidationResult`；CLI `python … [files…]`
  - `pipeline/scripts/extract_from_notebooklm.py` — yaml I/O 層：`load_book` / `save_book` / `pending_sources` / `pending_chapters`（含磁碟漂移偵測）/ `set_notebook_id` / `mark_source_ingested` / `mark_chapter_extracted` / `mark_chapter_generated` / `mark_chapter_reviewed`；CLI `status | plan`
- **驗證**:
  - 模板 slot 開合對稱
  - `validate_chapter.py` 對範例章節 exit 0
  - `extract_from_notebooklm.py plan _example.yaml` 正確列出 8 步（1 × create_notebook + 4 × ingest_source + 3 × extract_chapter）
  - `bash quality/validate.sh` 全過
- **備註**:
  - `chapter_prompt.md`（早就存在）是「呼叫 Claude 的 prompt 模板」；新的 `chapter.md.tmpl` 是「要填空的 scaffold」— 兩者互補，一個給 LLM 讀、一個給腳本字串替換
  - NotebookLM MCP 的實際呼叫仍由 Claude 在 session 中執行；`extract_from_notebooklm.py` 的角色是 **冪等書記**（檢查狀態、回寫 yaml），保證同任務在 fresh session 仍能接續
  - `pending_chapters` 會偵測磁碟漂移（yaml 還是 pending 但 `pipeline/extracted/{slug}.md` 已存在 → 當作已 extracted 跳過），避免重複呼叫 MCP
  - Python 腳本依賴 `PyYAML`（系統已安裝 6.0.1）；如要移植到 CI，記得在 requirements 中加上

---

## TASK-20260418-04: 生成學習路線圖與章節骨架
- **完成日期**: 2026-04-18
- **產出**:
  - `pipeline/sources/embodied-robotics.yaml` — 一本書 22 章 + 5 來源（C++ 性能優化、C++ 併發編程、ROS2 實踐、具身智能面試題、Sim-to-Real Survey）；含 `notebook_id` / `notebook_url` / `notebooklm_planning_session`
  - `content/embodied-robotics/{01..22}-*.md` — 22 個章節 scaffold，每個含 frontmatter（title、prerequisites 鏈、estimated_time=45、difficulty=3、tags、sidebar_position）與 8 必要段落占位
  - `content/embodied-robotics/_category_.json` — sidebar 類別設定（label、collapsed=false、generated-index）
  - `content/embodied-robotics/_meta.yaml` — 給 pipeline 用的書籍 metadata
  - 附帶：`src/components/TeX.tsx`（`katex.renderToString` 包裝；修首頁 PRINCIPLE I/II/III 的 math 欄位沒渲染的 bug）；`src/css/custom.css` 加 `.katex` 全面 reset 避免全站 `font-feature-settings` / `letter-spacing` 滲透進 math
- **驗證**:
  - `npm run build` 綠，sidebar 出現完整 22 章
  - 隨機抽檢 `/content/embodied-robotics/cpp-memory-optimization` — title、category、scaffold 都對
  - `/content/example/intro` 與首頁三條公式用 Playwright 實機驗過，確認 KaTeX 字型載入 (`document.fonts` 5 個 KaTeX_* loaded)、數學正確渲染
  - `bash quality/validate.sh` 22 章僅「練習題 0」的預期錯誤（TASK-05 會修）
- **備註**:
  - NotebookLM MCP 的 Chrome profile ≠ 使用者主瀏覽器登入。第一次查詢全炸是因為 MCP profile 沒登入 Google；`re_auth` 357 秒重登才通。Google cookie 一般約 30 天有效，之後會再卡一次
  - notebook url `/0502daf4-…`、session id `e6cb9a47` 記錄在 yaml，若要 TASK-05 沿用同一個對話脈絡可以傳 `session_id`
  - 自動生成的 sidebar 會剝 `01-` 前綴，路由是 `/content/embodied-robotics/cpp-memory-optimization`（不含 `01-`）；`sidebar_position` 已明確寫在 frontmatter，不靠檔名前綴排序
  - 首頁 KaTeX 用新的 `<TeX>` 元件，它走 `katex.renderToString` + `dangerouslySetInnerHTML`；之後其他 `.tsx` / `.mdx` 頁要寫 math 都用這個元件，別嵌 raw TeX 字串（字串不會被 Docusaurus 的 remark-math 處理，只有 `.md` 章節走那條管線）
